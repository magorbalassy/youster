---
kind: pipeline
type: docker
name: default

steps:
- name: deploy new Google Drive backend
  image: appleboy/drone-scp
  host: gc.maz.si
  user: gc.username
  key:
    from_secret: gc.password
  target: download_backend/
  source:
    - download_backend/download_backend.py
